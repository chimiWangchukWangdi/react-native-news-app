import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  chipItem: {
    borderColor: "#63B3ED",
    borderRadius: 11,
    borderWidth: 1,
    marginRight: 10,
    marginBottom: 0,
  },
  selectedChipItem: {
    backgroundColor: "#3182CE",
    borderColor: "#63B3ED",
  }
});
